@model CharBuildJsonRaw.Build

<div class="container mt-4">
    <h1>Character Build Data</h1>
    <p>App: @Model.BuiltWith.App</p>
    <p>Version: @Model.BuiltWith.Version</p>
    <p>Class: @Model.Class</p>
    <p>Origin: @Model.Origin</p>
    <p>Alignment: @Model.Alignment</p>
    <p>Name: @Model.Name</p>
    <p>Comment: @Model.Comment</p>

    <div class="row mt-4">
        <div class="col-4">
            <h2>Primary Powers</h2>
            @foreach (var powerSet in Model.PowerSets.Where(ps => ps.StartsWith("Brute_Melee")))
            {
                <div class="card my-2">
                    <div class="card-header">
                        <h5 class="mb-0">
                            <button class="btn btn-link" data-bs-toggle="collapse" data-bs-target="#powerSet@powerSet">
                                Power Set: @powerSet
                            </button>
                        </h5>
                    </div>

                    <div id="powerSet@powerSet" class="collapse">
                        <div class="card-body">
                            @foreach (var powerEntry in Model.PowerEntries.Where(pe => pe.PowerName.StartsWith(powerSet)))
                            {
                                <div class="card my-2">
                                    <div class="card-header">
                                        <h5 class="mb-0">
                                            <button class="btn btn-link" data-bs-toggle="collapse" data-bs-target="#powerEntry@powerEntry.PowerName">
                                                Power Name: @powerEntry.PowerName
                                            </button>
                                        </h5>
                                    </div>

                                    <div id="powerEntry@powerEntry.PowerName" class="collapse">
                                        <div class="card-body">
                                            <p class="level">Power Placed at Level: @powerEntry.Level</p>
                                            <p class="stat-include">Stat Include: @powerEntry.StatInclude</p>
                                            <!-- Add other properties as needed -->
                                            <button class="btn btn-secondary show-slots-btn" data-bs-toggle="collapse" data-bs-target="#slots@powerEntry.PowerName">Show Slots</button>
                                            <div id="slots@powerEntry.PowerName" class="collapse">
                                                <ul class="list-group">
                                                    @foreach (var slotEntry in powerEntry.SlotEntries)
                                                    {
                                                        <li class="list-group-item">Enhancement: @slotEntry.Enhancement?.EnhancementName</li>
                                                    }
                                                </ul>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            }
                        </div>
                    </div>
                </div>
            }
        </div>
        <div class="col-4">
            <h2>Secondary Powers</h2>
            @foreach (var powerSet in Model.PowerSets.Where(ps => ps.StartsWith("Brute_Defense")))
            {
                <div class="card my-2">
                    <div class="card-header">
                        <h5 class="mb-0">
                            <button class="btn btn-link" data-bs-toggle="collapse" data-bs-target="#powerSet@powerSet">
                                Power Set: @powerSet
                            </button>
                        </h5>
                    </div>

                    <div id="powerSet@powerSet" class="collapse">
                        <div class="card-body">
                            @foreach (var powerEntry in Model.PowerEntries.Where(pe => pe.PowerName.StartsWith(powerSet)))
                            {
                                <div class="card my-2">
                                    <div class="card-header">
                                        <h5 class="mb-0">
                                            <button class="btn btn-link" data-bs-toggle="collapse" data-bs-target="#powerEntry@powerEntry.PowerName">
                                                Power Name: @powerEntry.PowerName
                                            </button>
                                        </h5>
                                    </div>

                                    <div id="powerEntry@powerEntry.PowerName" class="collapse">
                                        <div class="card-body">
                                            <p class="level">Power Placed at Level: @powerEntry.Level</p>
                                            <p class="stat-include">Stat Include: @powerEntry.StatInclude</p>
                                            <!-- Add other properties as needed -->
                                            <button class="btn btn-secondary show-slots-btn" data-bs-toggle="collapse" data-bs-target="#slots@powerEntry.PowerName">Show Slots</button>
                                            <div id="slots@powerEntry.PowerName" class="collapse">
                                                <ul class="list-group">
                                                    @foreach (var slotEntry in powerEntry.SlotEntries)
                                                    {
                                                        <li class="list-group-item">Enhancement: @slotEntry.Enhancement?.EnhancementName</li>
                                                    }
                                                </ul>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            }
                        </div>
                    </div>
                </div>
            }
        </div>
        <div class="col-4">
            <h2>Mastery/Epic Powers</h2>
            <div class="row mt-4">
                <div class="col">
                    @foreach (var powerSet in Model.PowerSets.Where(ps => ps.Contains("mastery_") || ps.Contains("Epic")))
                    {
                        <div class="card my-2">
                            <div class="card-header">
                                <h5 class="mb-0">
                                    <button class="btn btn-link" data-bs-toggle="collapse" data-bs-target="#powerSet@powerSet">
                                        Power Set: @powerSet
                                    </button>
                                </h5>
                            </div>

                            <div id="powerSet@powerSet" class="collapse">
                                <div class="card-body">
                                    @foreach (var powerEntry in Model.PowerEntries.Where(pe => pe.PowerName.StartsWith(powerSet)))
                                    {
                                        <div class="card my-2">
                                            <div class="card-header">
                                                <h5 class="mb-0">
                                                    <button class="btn btn-link" data-bs-toggle="collapse" data-bs-target="#powerEntry@powerEntry.PowerName">
                                                        Power Name: @powerEntry.PowerName
                                                    </button>
                                                </h5>
                                            </div>

                                            <div id="powerEntry@powerEntry.PowerName" class="collapse">
                                                <div class="card-body">
                                                    <p class="level">Power Placed at Level: @powerEntry.Level</p>
                                                    <p class="stat-include">Stat Include: @powerEntry.StatInclude</p>
                                                    <!-- Add other properties as needed -->
                                                    <button class="btn btn-secondary show-slots-btn" data-bs-toggle="collapse" data-bs-target="#slots@powerEntry.PowerName">Show Slots</button>
                                                    <div id="slots@powerEntry.PowerName" class="collapse">
                                                        <ul class="list-group">
                                                            @foreach (var slotEntry in powerEntry.SlotEntries)
                                                            {
                                                                <li class="list-group-item">Enhancement: @slotEntry.Enhancement?.EnhancementName</li>
                                                            }
                                                        </ul>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    }
                                </div>
                            </div>
                        </div>
                    }
                </div>
            </div>

            <h2>Pool Powers</h2>
            <div class="row mt-4">
                <div class="col">
                    @foreach (var powerSet in Model.PowerSets.Where(ps => ps.StartsWith("Pool")))
                    {
                        <div class="card my-2">
                            <div class="card-header">
                                <h5 class="mb-0">
                                    <button class="btn btn-link" data-bs-toggle="collapse" data-bs-target="#powerSet@powerSet">
                                        Power Set: @powerSet
                                    </button>
                                </h5>
                            </div>

                            <div id="powerSet@powerSet" class="collapse">
                                <div class="card-body">
                                    @foreach (var powerEntry in Model.PowerEntries.Where(pe => pe.PowerName.StartsWith(powerSet)))
                                    {
                                        <div class="card my-2">
                                            <div class="card-header">
                                                <h5 class="mb-0">
                                                    <button class="btn btn-link" data-bs-toggle="collapse" data-bs-target="#powerEntry@powerEntry.PowerName">
                                                        Power Name: @powerEntry.PowerName
                                                    </button>
                                                </h5>
                                            </div>

                                            <div id="powerEntry@powerEntry.PowerName" class="collapse">
                                                <div class="card-body">
                                                    <p class="level">Power Placed at Level: @powerEntry.Level</p>
                                                    <p class="stat-include">Stat Include: @powerEntry.StatInclude</p>
                                                    <!-- Add other properties as needed -->
                                                    <button class="btn btn-secondary show-slots-btn" data-bs-toggle="collapse" data-bs-target="#slots@powerEntry.PowerName">Show Slots</button>
                                                    <div id="slots@powerEntry.PowerName" class="collapse">
                                                        <ul class="list-group">
                                                            @foreach (var slotEntry in powerEntry.SlotEntries)
                                                            {
                                                                <li class="list-group-item">Enhancement: @slotEntry.Enhancement?.EnhancementName</li>
                                                            }
                                                        </ul>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    }
                                </div>
                            </div>
                        </div>
                    }
                </div>
            </div>

            <h2>Inherent Powers</h2>
            <div class="row mt-4">
                <div class="col">
                    @foreach (var powerSet in Model.PowerSets.Where(ps => ps.StartsWith("Inherent")))
                    {
                        <div class="card my-2">
                            <div class="card-header">
                                <h5 class="mb-0">
                                    <button class="btn btn-link" data-bs-toggle="collapse" data-bs-target="#powerSet@powerSet">
                                        Power Set: @powerSet
                                    </button>
                                </h5>
                            </div>

                            <div id="powerSet@powerSet" class="collapse">
                                <div class="card-body">
                                    @foreach (var powerEntry in Model.PowerEntries.Where(pe => pe.PowerName.StartsWith(powerSet)))
                                    {
                                        <div class="card my-2">
                                            <div class="card-header">
                                                <h5 class="mb-0">
                                                    <button class="btn btn-link" data-bs-toggle="collapse" data-bs-target="#powerEntry@powerEntry.PowerName">
                                                        Power Name: @powerEntry.PowerName
                                                    </button>
                                                </h5>
                                            </div>

                                            <div id="powerEntry@powerEntry.PowerName" class="collapse">
                                                <div class="card-body">
                                                    <p class="level">Power Placed at Level: @powerEntry.Level</p>
                                                    <p class="stat-include">Stat Include: @powerEntry.StatInclude</p>
                                                    <!-- Add other properties as needed -->
                                                    <button class="btn btn-secondary show-slots-btn" data-bs-toggle="collapse" data-bs-target="#slots@powerEntry.PowerName">Show Slots</button>
                                                    <div id="slots@powerEntry.PowerName" class="collapse">
                                                        <ul class="list-group">
                                                            @foreach (var slotEntry in powerEntry.SlotEntries)
                                                            {
                                                                <li class="list-group-item">Enhancement: @slotEntry.Enhancement?.EnhancementName</li>
                                                            }
                                                        </ul>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    }
                                </div>
                            </div>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>


</div>

@section Scripts {

}
